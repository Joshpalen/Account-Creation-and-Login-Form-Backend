version: '3.8'
services:
  backend:
    build: .
    ports:
      - '3000:3000'
    volumes:
      - ./database.sqlite:/app/database.sqlite
    environment:
      - NODE_ENV=production
      - PORT=3000
      - JWT_SECRET=change_me
